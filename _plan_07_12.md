# advanced_tutorial 07-12 讲扩展计划

> 供下一次对话直接执行。生成日期：2026-08-25。
> 依据：对四套现有教程（langchain_tutorial / langgraph_tutorial / rag_tutorial / advanced_tutorial 01-06）的知识缺口分析。

## 一、目标与范围

在 `advanced_tutorial/` 追加 **07-12 共 6 讲**，补齐现有教程未覆盖的 6 大优先知识点：

| 讲次 | 主题 | 对应缺口 |
|---|---|---|
| 07 | LLM 缓存与成本控制 | 缓存体系（现有教程完全未覆盖） |
| 08 | LangGraph Functional API | `@entrypoint` / `@task` 新编排范式 |
| 09 | Human-in-the-loop 完整版 | `interrupt()` / `Command(resume=)` 人工介入 |
| 10 | RAG 进阶检索三件套 | SelfQuery / ParentDocument / HyDE |
| 11 | 部署与可观测 | LangServe（或 FastAPI 降级）+ LangSmith |
| 12 | 实战：生产级 RAG 服务 | 贯穿项目，串联 07-11 全部知识点 |

**不含**（明确排除，避免范围膨胀）：100+ 文档加载器、OutputParser 家族、多模态、RAPTOR/GraphRAG、安全防护——留作后续扩展。

## 二、第一步：风险验证（写代码前必做）

当前环境为 **langchain 1.3.14 / langgraph 1.2.10 / langchain-classic**（见根 README "已验证事项"）。
以下 API 均需在 notebook 中**先实测**再写入教程，把实际导入路径与行为记录进各讲 FAQ：

| # | 待验证项 | 预期导入 | 风险 |
|---|---|---|---|
| 1 | LLM 缓存 | `from langchain_core.caches import InMemoryCache, SQLiteCache` + `model.cache = ...` | 命中条件（消息序列完全一致）需实测；流式下命中行为需实测 |
| 2 | Functional API | `from langgraph.func import entrypoint, task` | langgraph 1.2.10 的 API 形态（task 返回值、entrypoint 的 checkpointer 参数） |
| 3 | interrupt/resume | `from langgraph.types import interrupt, Command` | 必须配 checkpointer；resume 值进入节点的方式 |
| 4 | SelfQueryRetriever | 预计 `langchain_classic.retrievers.self_query`（community 已移除经典检索器） | 导入路径坑，与 01 讲 FAQ 同源 |
| 5 | ParentDocumentRetriever | 预计 `langchain_classic.retrievers` | 同上 |
| 6 | HyDE | `langchain_classic.chains.hyde` 或直接 LCEL 手写（推荐手写，教学更清晰） | 旧 API 可能已弃用 |
| 7 | langserve | `pip show langserve`；官方已进**维护模式** | 若兼容性问题大 → 降级为 FastAPI + SSE 手写方案（项目已有 `ai_qa_app/main.py` 与 `langchain_tutorial/11_SSE详解.ipynb` 可参考） |
| 8 | LangSmith | 无 key 时需降级方案 | 无 `LANGSMITH_API_KEY` 时改为 `astream_events` 自建日志示例 + LangSmith 概念讲解 |

新增依赖（按实测结果执行）：`uv add langserve fastapi uvicorn`（仅 11/12 讲需要）。

## 三、讲次详细大纲

每讲结构沿用现有 01-06 风格：**首格初始化（HEADER_CODE）→ 概念 markdown → 代码 cell（含预期输出注释）→ 要点小结 → FAQ 表格**。

### 07_LLM缓存与成本控制.ipynb

- **知识点**：
  - 为什么需要缓存：相同请求二次调用零成本、零延迟
  - `model.cache = InMemoryCache()`（实例级）vs `set_llm_cache()`（全局）
  - `SQLiteCache` 持久化缓存（跨进程重启）
  - 缓存命中条件：消息序列逐字一致（实测验证改一个字是否失效）
  - `.with_retry()` 链级自动重试（与 01 讲 `with_fallbacks` 对比：retry 同一链 / fallback 换链）
  - `usage_metadata` 读取 token 消耗做成本统计
- **核心示例**：同一问题连续 invoke 两次，打印两次耗时对比（第二次毫秒级返回）；带 temperature 不同值测试命中。
- **FAQ 预设**：为什么改一个字就不命中；缓存命中时 stream() 的行为；SQLite 文件位置与清理。

### 08_LangGraph_Functional_API.ipynb

- **知识点**：
  - `@entrypoint` / `@task` 装饰器：无显式 State schema 的函数式编排
  - task 的并行执行（多个 task 直接并发 await）
  - `entrypoint(checkpointer=...)` 持久化与 `entrypoint` 的状态读写（`.value` / 自定义 reduce）
  - **与 StateGraph 的选型对比表**：结构复杂/状态显式 → StateGraph；线性流水线/快速原型 → Functional API
- **核心示例**：用 Functional API 重写 02 讲的"多角度评审"（与 Send 模式对照，同一问题两种实现）。
- **FAQ 预设**：task 内能否调 LLM/工具；entrypoint 嵌套；与 StateGraph 混用（graph 作为 task）。

### 09_Human_in_the_loop完整版.ipynb

- **知识点**：
  - `interrupt()` 动态暂停：节点内部调用，抛出暂停点等待外部输入
  - `Command(resume=...)` 恢复执行：resume 值如何回到 interrupt 调用处
  - 前提条件：**必须编译时传 checkpointer**，否则报错（配 InMemorySaver + thread_id）
  - 静态断点 `interrupt_before` / `interrupt_after` vs 动态 `interrupt()` 的适用场景
  - `graph.get_state()` 查看暂停现场；`update_state()` 人工改状态后继续（衔接 03 讲时间旅行）
  - 审批拒绝路径：resume 传"拒绝"→ 节点内分支处理
- **核心示例**：内容审核 Agent——LLM 生成文案后 interrupt 等人工批准；演示"批准发布"与"拒绝重写"两条路径。
- **FAQ 预设**：无 checkpointer 报什么错；一次运行多次 interrupt 如何定位；resume 传复杂对象（dict/Pydantic）。

### 10_RAG进阶检索三件套.ipynb

- **知识点**：
  - **SelfQueryRetriever**：LLM 从自然语言提取"语义查询 + 元数据过滤"（如"2023 年后关于部署的文档"）
  - **ParentDocumentRetriever**：小 chunk 做检索（精度高）、大 chunk 送 LLM（上下文全），父子存储结构
  - **HyDE**：先让 LLM 生成"假设性答案文档"，用它做向量检索（缓解 query 与 doc 的表述差异）
  - 检索质量对比实验：baseline 向量检索 vs 三件套，同一问题集对比召回内容
- **核心示例**：复用 `rag_qa_project/data/` 的文档 + Chroma（`bge-small-zh-v1.5` 嵌入，首格设 `HF_ENDPOINT` 镜像）；SelfQuery 示例需给文档加 `year`/`topic` 元数据字段。
- **FAQ 预设**：导入路径坑（langchain_classic）；SelfQuery 对模型结构化能力的要求；HyDE 每次多一次 LLM 调用的成本取舍。

### 11_部署与可观测.ipynb

- **知识点**：
  - **方案 A（首选，实测通过才用）**：LangServe `add_routes(app, chain, path="/rag")`，`playground` 端点体验
  - **方案 B（降级方案）**：FastAPI + `astream` + SSE 手写流式端点（呼应 `langchain_tutorial/11_SSE详解`），并注明 LangServe 已进维护模式、官方重心转向 LangGraph Platform
  - 客户端调用：`RemoteRunnable`（LangServe）或 httpx/requests（方案 B）
  - **LangSmith**：环境变量配置（`LANGSMITH_TRACING`/`LANGSMITH_API_KEY`）、追踪界面讲解（截图式文字描述）；无 key 降级：`astream_events` 自建结构化日志
- **核心示例**：把 rag_tutorial/05 风格的 RAG 链部署成 uvicorn 本地服务，notebook 内用子进程起服务 + 客户端 cell 调用 + 收尾关闭进程。
- **FAQ 预设**：notebook 内起 uvicorn 的端口冲突处理；CORS；流式端点的 media type；LangServe 与 langgraph 1.x 的兼容边界。

### 12_实战_pro_rag_service（.py 项目，非 notebook）

贯穿项目，**串联 07-11 全部知识点**。目录结构（对齐现有 `rag_qa_project/` 惯例）：

```
advanced_tutorial/pro_rag_service/
├── config.py        # pydantic-settings：模型/缓存/检索/服务配置（呼应 06 讲分层）
├── ingest.py        # 入库：ParentDocument 策略 + year/topic 元数据（10 讲）
├── graph.py         # Functional API 编排（08 讲）：检索 → interrupt 审批（09 讲）→ 生成
├── cache.py         # LLM 缓存 + usage_metadata 成本统计（07 讲）
├── server.py        # FastAPI/LangServe 部署（11 讲），含 SSE 流式端点
├── evaluate.py      # 评估脚本：无 LangSmith key 时本地跑固定问题集 + 断言式检查
├── tests/           # pytest 离线测试（mock LLM，对齐 rag_qa_project/tests 风格）
├── data/            # 复用 rag_qa_project/data 或新增样例文档
└── README.md        # 运行指引（ingest → serve → evaluate 三步）
```

**验收流程**：`pytest tests/` 全过 → `python ingest.py` 入库 → `python server.py` 起服务 → 审批流端到端演示（提问 → 暂停审批 → resume → 流式回答）→ `python evaluate.py` 出报告。

## 四、执行方式

1. **新建 `_build_pro.py`**（复用 `_build_advanced.py` 的 `md()/code()/dump()/HEADER_CODE/HEADER_MD` 模板，直接 import 或复制），生成 07-11 五个 notebook。**不改动已验证的 `_build_advanced.py`**。
2. 第 12 讲为 .py 项目，直接写文件（不经 notebook 生成）。
3. 更新 `advanced_tutorial/README.md`：模块地图追加 07-12 行、学习路径更新为 `01 → ... → 06 → 07-11 → pro_rag_service`。
4. 每讲生成后**逐 cell 实际执行**验证（真实 DeepSeek），把实测结果回填为 cell 输出 + 修正 FAQ。

## 五、验收标准

- [ ] 07-11 每个 notebook 第一格自带初始化、可独立运行
- [ ] 所有导入路径基于 langchain 1.3.14 / langgraph 1.2.10 实测（无 DeprecatedWarning 私有 API）
- [ ] 每讲末尾 FAQ 表格记录版本坑（延续现有教程惯例）
- [ ] pro_rag_service：pytest 全过 + 三步运行指引 + 端到端审批流演示成功
- [ ] README.md 模块地图与实际文件一致

## 六、执行顺序

```
风险验证（二节表格 8 项，输出实测结论）
  → 07 缓存 → 08 Functional API → 09 interrupt/resume
  → 10 检索三件套 → 11 部署可观测 → 12 pro_rag_service
  → README 更新 → 全量复跑验证
```

## 七、给执行者的上下文提示

- 项目根目录 `.env` 需已有 `DEEPSEEK_API_KEY`；嵌入走 `HF_ENDPOINT=https://hf-mirror.com`（必须在 langchain 导入前设置，见 rag_qa_project FAQ）。
- `model` 变量约定：`ChatDeepSeek(model="deepseek-chat", temperature=0.2)`，与 01-06 讲一致。
- 经典检索器一律从 `langchain_classic.retrievers` 导入；`create_agent` 在 `langchain.agents`。
- 02 讲已讲过 Send 并行与哨兵节点、03 讲已讲过 update_state/时间旅行——08/09 讲直接引用这些前置，不重复展开。
- 教程语言为中文，代码注释中文，风格对齐 01-06 讲。
